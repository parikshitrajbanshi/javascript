//Both indexOf(), and lastIndexOf() return -1 if the text is not found:

let text = "Please locate where locate  occurs!";
let str=text.lastIndexOf("locate");

console.log(str);

function love(str){
    return str.lastIndexOf('text');
}

let sum=love('all of the text are written in text language');
console.log(sum);

//Both indexOf(), and lastIndexOf() return -1 if the text is not found://

function lo(str){
    return str.lastIndexOf('text',1);
}

let um = lo('in text language');
console.log(um);